node_modules/
coverage/
*.tsbuildinfo
.DS_Store
*.log
.claude/
# NOTE: dist/ is intentionally committed (the runtime bundle the target runs).
# FPF reference spec (9.7MB) — an external input, not plugin source; kept local.
FPF-Spec.md
