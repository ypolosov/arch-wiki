#!/usr/bin/env bash
# Thin shim: run the bundled deterministic CLI. Added to PATH when the plugin is
# enabled, so commands/agents call `arch-wiki ...` instead of a node path.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec node "$ROOT/dist/cli.cjs" "$@"
